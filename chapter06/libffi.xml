<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-libffi" role="wrap">
  <?dbhtml filename="libffi.html"?>

  <sect1info condition="script">
    <productname>libffi</productname>
    <productnumber>&libffi-version;</productnumber>
    <address>&libffi-url;</address>
  </sect1info>

  <title>Libffi-&libffi-version;</title>

  <indexterm zone="ch-system-libffi">
    <primary sortas="a-libffi">libffi</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Libffi 库为各种调用规范提供了一个可移植的，高级编程接口。允许程序员在运行时，通过调用接口描述调用任意指定函数。</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&libffi-ch6-sbu;</seg>
        <seg>&libffi-ch6-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>安装 Libffi</title>

    <note>
      <para>Libffi 与 GMP 相似，构建时会根据使用的处理器优化。如果需要构建的是另一个系统，设定 CFLAGS 和 CXXFLAGS 为你的架构指定成通用构建。如果不这样做，所有指向 libffi 的链接将触发非法操作错误。</para>
    </note>

    <para>修改 Makefile 将头文件安装到标准的 <filename class="directory">/usr/include</filename> 目录，而非 <filename class="directory">/usr/lib/libffi-&libffi-version;/include</filename>。</para>

<screen><userinput remap="pre">sed -e '/^includesdir/ s/$(libdir).*$/$(includedir)/' \
    -i include/Makefile.in

sed -e '/^includedir/ s/=.*$/=@includedir@/' \
    -e 's/^Cflags: -I${includedir}/Cflags:/' \
    -i libffi.pc.in</userinput></screen>

    <para>编译 libffi 前的准备：</para>

<screen><userinput remap="configure">./configure --prefix=/usr --disable-static --with-gcc-arch=native</userinput></screen>

    <variablelist>
      <title>配置选项的含义：</title>

      <varlistentry>
        <term><parameter>--with-gcc-arch=native</parameter></term>
        <listitem>
          <para>确保 GCC 为当前系统进行优化。如果没有指定则会进行猜测，这样生成的代码在某些系统上是不正确的。如果生成的代码想要从本机系统复制到功能较弱的系统，则应该以功能较弱的系统作为参数。有关替代系统类型的详细信息，请参阅 <ulink url='https://gcc.gnu.org/onlinedocs/gcc-&gcc-version;/gcc/x86-Options.html'>gcc 手册中的 x86 选项</ulink>。</para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para>编译软件包：</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>用以下命令测试结果：</para>

<screen><userinput remap="test">make check</userinput></screen>

    <para>安装软件包：</para>

<screen><userinput remap="install">make install</userinput></screen>

  </sect2>

  <sect2 id="contents-libffi" role="content">
    <title>Libffi 的软件包内容</title>

    <segmentedlist>
      <segtitle>安装的库</segtitle>

      <seglistitem>
        <seg>libffi.so</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">简要介绍</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="libffi">
        <term><filename class="libraryfile">libffi</filename></term>
        <listitem>
          <para>包含 libffi 的 API 函数。</para>
          <indexterm zone="ch-system-libffi">
            <primary sortas="c-libffi">libffi</primary>
          </indexterm>
        </listitem>
      </varlistentry>
    </variablelist>

  </sect2>

</sect1>

