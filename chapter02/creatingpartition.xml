<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="space-creatingpartition">
  <?dbhtml filename="creatingpartition.html"?>

  <title>创建新分区</title>

  <para>与绝大多数其它操作系统相同，安装 LFS 通常需要专门的分区。构建 LFS 系统比较推荐的方法是使用可用的空分区，或者如果条件允许，最好是在未分区的空间里新建分区。</para>

<!--

  <para>It is possible to install an LFS system (in fact even multiple LFS
  systems) on a partition already occupied by another
  operating system and the different systems will co-exist peacefully. The
  document <ulink url="&hints-root;lfs_next_to_existing_systems.txt"/>
  contains notes on how to implement this. This document was last updated
  in 2004. It has not been updated since and it has not been tested with
  recent versions of this LFS book. The document is more than likely not
  usable as-is and you will need to account for changes made to the LFS
  procedures since it was written. This is only recommended for expert LFS
  users.</para>

-->

  <para>最小化的系统需要大约 4 GB 的分区，这足以存储所有的源码包及满足编译的需求。但如果要将 LFS 作为主要的 Linux 系统，可能需要安装其它附加的软件，这将需要额外的空间。考虑到了日后所需的空间，一个 10 GB 的分区是比较合理的。LFS 系统本身并不会占用这么多的空间，但大分区将能提供充裕的临时储存空间。编译软件包可能需要较大的磁盘空间，但这些空间可以在软件包安装后回收。</para>

  <para>由于编译过程中所需的内存（RAM）可能不足，用一个小型的磁盘分区作为 <systemitem class="filesystem">swap</systemitem>空间是个不错的想法。内核会在此分区中储存较少使用的数据，从而为活动进程提供更多的内存。LFS 系统可以与宿主系统共用 <systemitem class="filesystem">swap</systemitem>  分区， 这样就没有必要再新建一个了。</para>

  <para>启动磁盘分区程序，通过如 <command>cfdisk</command> 或 <command>fdisk</command> 加上新分区所在的磁盘名&mdash;例如 <filename class="devicefile">/dev/sda</filename> 若是主盘为 IDE 的话。你需要创建一个 Linux 本地分区，并按需创建 <systemitem class="filesystem">swap</systemitem> 分区。如果你还不知道如何使用这些程序，请参考 <filename>cfdisk(8)</filename> 或 <filename>fdisk(8)</filename> 。</para>

  <note><para>对于有经验的用户，也可自行定制分区分案。 新版 LFS 系统支持软件 <ulink url="&blfs-book;postlfs/raid.html">RAID</ulink> 阵列或 <ulink url="&blfs-book;postlfs/aboutlvm.html">LVM</ulink> 然而，这些方案需要用到<ulink url="&blfs-book;postlfs/initramfs.html">initramfs</ulink>，这涉及到比较复杂的话题。并不建议首次尝试 LFS 的用户使用这样的分区方法。</para></note>

  <para>请记住新分区的位置（例如， <filename class="devicefile">sda5</filename>）。 本书中将称其为 LFS 分区。还需要记住 <systemitem class="filesystem">swap</systemitem> 分区的位置。这些将会在  <filename>/etc/fstab</filename> 文件中用到。</para>

  <sect2>
  <title>其它分区问题</title>

  <para>LFS 邮件列表中经常有人问到关于系统分区的建议。这一话题非常主观。大多数发行版默认情况下会使用整个磁盘，仅为交换分区保留一小部分空间，但由于种种原因，这并不适合 LFS。这样做会降低灵活性，使得多个发行版或几个 LFS 版本之间共享数据变得困难，也让备份更耗时，还会导致文件系统结构分配不合理而浪费磁盘空间。</para>

    <sect3>
    <title>根分区</title>

    <para>为 LFS 根分区（不要与 <filename class="directory">/root</filename> 目录混淆）分配 10 GB 的空间是比较好的折中方案。这为构建 LFS 和大多数 BLFS 提供了足够的空间，也小到可以轻易创建多个分区用于实验。</para> </sect3>

    <sect3>
    <title>交换分区</title>

    <para>大多数发行版会自动创建交换分区。一般来说，交换分区的推荐大小为物理内存的两倍左右，然而鲜少需要这样做。若是磁盘空间有限，可以设置为 2 GB的交换分区并查看磁盘的交换量。</para>

    <para>发生内存交换其实并不好。通常，你只需要观察磁盘活动以及系统对命令的响应程度就能知道这个系统是否在进行交换。通常在使用非常不合理的命令时才会发生交换，如尝试编辑一个大小为 5 GB 的文件时。如果交换在你的系统上是常态，那最好的办法就是为你的系统添置更多的物理内存。</para> </sect3>

    <sect3>
    <title>常用分区</title>

    <para>在分配磁盘时，有些分区不是必须的，但却值得你考虑。以下列表并不全面，仅供参考。</para>

    <itemizedlist>

      <listitem><para>/boot &ndash; 强烈推荐。此分区用于存储内核和其它启动信息。为了减少大容量磁盘启动时的潜在问题，尽量将该分区设为磁盘驱动器上第一个物理分区。100 MB 的空间就十分充裕了。</para></listitem>

      <listitem><para>/home &ndash; 强烈推荐。home 目录可用于跨发行版或多个 LFS 版本之间共享用户自定义内容。应该将尽量多的磁盘都分配给 home 分区。</para></listitem>

      <listitem><para>/usr &ndash; 独立的 /usr 分区常见于瘦客户端服务器或无盘工作站。LFS 通常不需要。5 GB 大小足以应付大部分安装。</para></listitem>

      <listitem><para>/opt &ndash; 该目录常用于在 BLFS 中，安装多个像 Gnome 或 KDE 这样无需将文件嵌入 /usr 层次结构的大型软件包。如果使用的话，5 到 10 GB 的空间就足够了。</para>
      </listitem>

      <listitem><para>/tmp &ndash;  独立的 /tmp 分区是比较少见的，但这在配置瘦客户端时会有用。如果使用的话，很少超过几 GB。</para></listitem>

      <listitem><para>/usr/src &ndash; 该分区非常用于存储 BLFS 源文件并在构建不同版本的 LFS 中共享。它也可用于构建 BLFS 软件包。30-50 GB 的分区可以提供足够的空间。</para></listitem>

    </itemizedlist>

    <para>任何你需要在启动时自动挂载的单独分区都需要写入到 <filename>/etc/fstab</filename> 文件中。有关如何指定分区的细节将在 <xref linkend="ch-bootable-fstab"/>中讨论 。</para>

    </sect3>
  </sect2>
</sect1>
